# ---- SECRETS — never commit -------------------------------------------------
.env
.env.*
# the template (no real values) IS committed:
!.env.example
*.pem
*.key

# ---- PRIVATE live financial data (your real book, orders, state) ------------
bot/state.json
bot/desk/positions.json
bot/desk/positions_prev.json
bot/desk/state.json
bot/desk/strategist_state.json
bot/desk/coverage.json
bot/desk/plan_*.json
bot/desk/_*.json
bot/logs/
bot/runs/
bot/reports/

# ---- environment / caches ---------------------------------------------------
.venv/
__pycache__/
*.py[cod]
.DS_Store

# ---- local editor/tooling ---------------------------------------------------
.claude/settings.local.json

# (TradingAgents/ is vendored and committed — see TradingAgents/VENDORED.md)
